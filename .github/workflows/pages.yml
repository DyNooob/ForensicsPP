# Forensics++ (ForensicsPP.com)
# Local-first browser forensics workbench
#
# Copyright (c) 2026 DyNooob. All rights reserved.
# Author: DyNooob
# Website: https://www.forensicspp.com
# Platform: DigiForensics.cn
# Project: https://github.com/DyNooob/ForensicsPP
#
# Forensics++ is an open-source, browser-side toolkit for CTF/MISC,
# lightweight forensic triage, encoding/decoding, metadata inspection,
# hashes, archive parsing, and local analysis.
#
# Do not use this project for unauthorized access, intrusion,
# privacy infringement, or unlawful activity.
#
# Released under the MIT License.
# Full source code: https://github.com/DyNooob/ForensicsPP

name: Deploy static build

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test and build production site
        run: npm run verify

      - name: Verify deployment artifact
        shell: bash
        run: |
          test -f dist/index.html
          test -f dist/CNAME
          test ! -e dist/.git
          test -z "$(find dist -type d -name .git -print -quit)"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

# The canonical site is deployed to GitHub Pages from the built `dist/` artifact
# produced by this workflow. Build verification runs before any deployment.
